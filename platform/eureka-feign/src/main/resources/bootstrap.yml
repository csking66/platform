spring:
  application:
    name: eureka-feign
  session:
    store-type: redis
  redis:
    host: 127.0.0.1
    port: 6379
    database: 0
    pool:
      max-idle: 20
      min-idle: 1
      max-active: 20
      max-wait: 60000
      database: 0
      timeout: 60000